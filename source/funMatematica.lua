local matematica = {}

matematica.add = function(x, y)
	return x + y
end

matematica.sub = function(x, y)
	return x - y
end

matematica.mul = function(x, y)
	return x * y
end

matematica.div = function(x, y)
	return x / y
end

return matematica